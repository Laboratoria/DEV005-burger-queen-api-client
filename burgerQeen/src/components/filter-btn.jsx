/* 
import Button from "./buttons";
const FilterBtn = ({ onMenuTypeChange }) => {
  const buttonClick = (menuType) => {
    onMenuTypeChange(menuType);
  };

  return (
    <div className="filterBtn">
      <Button
        className="break"
        text="Desayuno"
        onClick={() => buttonClick('desayuno')}
      />
      
      <Button
        className="break"
        text="Almuerzo"
        onClick={() => buttonClick('almuerzo')}
      />
      
      <Button
        className="break"
        text="Pedidos"
      />
      
    </div>
  );
};

export default FilterBtn;
 */